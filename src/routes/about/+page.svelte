<script>
  /**
   * Never rendered: the load function in +page.js always redirects to `/`,
   * where the About prose now lives. The file exists because a route needs a
   * +page.svelte to be a route at all, and this one has to stay a route so old
   * /about links keep resolving.
   */
</script>
